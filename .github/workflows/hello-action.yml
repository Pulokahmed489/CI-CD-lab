name: Hello CI/CD

on:
  push:
    branches:
      - main

jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello to Github
        run: echo "Hello, CI/CD with GitHub Actions!"
      - name: Say Hello to Hassan
        run: echo "Hello, Hassan From GitHub Actions!"
